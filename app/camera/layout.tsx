export const metadata = {
  title: "Kamera - DermaSense",
  description: "Neue Hautanalyse durchführen",
};

export default function CameraLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
