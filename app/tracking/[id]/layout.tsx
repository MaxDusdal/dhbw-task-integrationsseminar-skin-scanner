export const metadata = {
  title: "Läsions-Tracking - DermaSense",
  description: "Verlaufsdokumentation einzelner Hautveränderungen",
};

export default function TrackingLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
